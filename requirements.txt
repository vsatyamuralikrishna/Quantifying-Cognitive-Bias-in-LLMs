# Core packages used in qcbai/ and main.py
# For conda: conda install -c conda-forge numpy matplotlib pydantic tqdm pyyaml
# Then: pip install ollama

# ============================================================================
# Direct dependencies (used in code)
# ============================================================================

# Data science and visualization
numpy>=1.24
matplotlib>=3.7

# Data validation (pydantic v2)
pydantic>=2.10.0

# Utilities
tqdm>=4.67.0
pyyaml>=6.0

# LLM client
ollama>=0.1.0

# ============================================================================
# Pydantic dependencies (automatically installed with pydantic via conda)
# Included here for pip installs or explicit control
# ============================================================================
annotated-types>=0.7.0
typing-extensions>=4.12.0
pydantic-core>=2.27.0

# ============================================================================
# Optional packages (uncomment if needed)
# ============================================================================
# openai>=1.59.0
# python-dotenv>=1.0.0

# High-performance inference (for vLLM backend)
# vllm>=0.2.0  # Uncomment to enable vLLM support (requires CUDA)
